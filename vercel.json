{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "next build"
      }
    }
  ],
  "env": {
    "SKIP_PRISMA_MIGRATIONS": "true"
  }
}
